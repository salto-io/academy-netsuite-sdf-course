# What version of JDK is installed?
javac --version

# What version of node.js is installed?
node --version

# What version of npm is installed?
npm --version

# Install SDF globally, prompt for TOS acceptance
npm install -g @oracle/suitecloud-cli

# or install SDF globally, automatically accept TOS
npm install -g --acceptSuiteCloudSDKLicense @oracle/suitecloud-cli

# What version of SDF is installed?
suitecloud --version
